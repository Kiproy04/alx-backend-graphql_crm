# CRM Weekly Report Setup

This document explains how to set up Celery and Celery Beat to generate weekly CRM reports.

## 1. Install Dependencies

```bash
pip install -r requirements.txt
sudo apt install redis-server  # on Linux
